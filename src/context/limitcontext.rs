use crate::runner::{Runner, RunContext, RunContextWrap, PixBuffer};

/*###

pub struct LimitRunner {
    pub runner: Box<dyn Runner>,
    limit: f32,
}

impl LimitRunner {
    pub fn new(runner: dyn Runner, limit: f32) -> LimitRunner {
        LimitRunner {
            runner: Box::new(runner),
            limit: limit,
        }
    }
}

###*/
